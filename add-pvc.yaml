---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-site
  namespace: development
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
